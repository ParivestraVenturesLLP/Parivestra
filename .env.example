# Email notifications (existing contact form)
SMTP_HOST=
SMTP_PORT=
SMTP_USER=
SMTP_PASS=
ADMIN_EMAIL=

# Supabase (leads storage) — service role key, server-only, never expose to the frontend
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=

# Admin dashboard (/admin) — single shared password
ADMIN_DASHBOARD_PASSWORD=
